# Rare Oud Terraform Readiness

This directory is an IaC scaffold for managed MySQL, Redis, object storage, container services, CDN/WAF, and observability. Keep secrets in the cloud secret manager, not in Terraform variables committed to git.
